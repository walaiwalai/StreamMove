# Codex 文档导航

## 远程服务器

- `144.24.36.160` 是服务器操作的跳板机。
- 登录跳板机后进入 `/home/admin/oracle`，执行 `sh connect-158.sh` 连接 158 服务器。
- 登录跳板机后进入 `/home/admin/oracle`，执行 `sh connect-2.sh` 连接 213 服务器。
- 检查远端任务前，先确认任务实际运行在 158 或 213 服务器，不要把跳板机上的进程和文件误认为目标机状态。

本文件只作为项目文档入口。处理任务时按需读取对应文档，不要一次加载与当前任务无关的内容。

## 文档索引

- [项目架构](mydocs/architecture/项目架构.md)：项目定位、技术版本、模块职责、核心流程和构建命令。分析跨模块影响、设计功能或修改核心流程前阅读。
- [编码规范](mydocs/standards/编码规范.md)：Java 与 Spring 编码要求。新增、修改或审查代码前必须阅读并遵守。
- [CR规范](mydocs/standards/CR规范.md)：代码审查与提交前自检要求。执行代码审查、重构或冗余清理时必须阅读并遵守。
- [外部服务接入规范](mydocs/standards/外部服务接入规范.md)：外部接口、SDK 和平台接入约束。涉及外部服务时必须先阅读。
- [AI高光识别框架](mydocs/features/highlight/AI高光识别框架.md)：高光系统的上位架构、通用核心、游戏适配器、偏好学习和反馈闭环。设计或调整高光方向前优先阅读。
- [低成本高光识别技术架构与实施路线](mydocs/features/highlight/低成本高光识别技术架构与实施路线.md)：优先复用现有模型的分层架构、成本边界、分阶段验收指标与训练触发条件。从头规划或收敛高光实现路线时阅读。
- [三角洲行动高光识别适配评估](mydocs/features/highlight/adapters/三角洲行动高光识别适配评估.md)：烽火地带测试素材预检、公开模型适用性、HUD事件定义和最小验证计划。处理三角洲行动素材或评估专项模型时阅读。
- [带弹幕游戏直播高光筛选流程](mydocs/features/highlight/带弹幕游戏直播高光筛选流程.md)：多模态召回、事件重建、偏好排序、剪辑验收和成本模型。修改弹幕高光流程前阅读。
- [高光离线实验记录](mydocs/experiments/highlight/)：按日期保存可复现的全场实验、用量、结果和待验证结论。分析既有实验效果或继续调优前读取对应记录。

## 使用规则

- 只读取与当前任务相关的文档；不确定影响范围时先阅读项目架构。
- 新增或修改代码时，项目编码规范和 CR 规范优先于通用编码习惯；完成修改前按照 CR 规范自检本次变更。
- 用户的明确要求与项目文档冲突时，先指出冲突和影响，再按照用户确认后的要求执行。
- 后续交互中，用户提出具有长期性、可复用性和项目级适用范围的编码或 CR 偏好时，应在完成当前任务的同时主动归纳并补充到对应规范；新增主题文档时同步更新本索引。
- 一次性处理方式、仅适用于当前上下文的取舍或尚未确认的建议，不写入长期规范。新规则与既有规则重复或冲突时，优先合并和修订原规则；无法判断是否应长期采用时先向用户确认。
- 动态补充规范后，应在交付说明中指出新增或调整了哪些规则。

## Codex 临时文件与实验产物

- 项目仓库只保存正式源代码、可复用测试代码、运行所需模型、项目文档和必要配置；
  禁止把一次性脚本、抽帧、接触表、临时音视频、模型原始返回、运行日志或离线实验
  产物写入 `StreamerRecord` 或其他项目仓库；
- Codex 一次性代码和中间文件统一写入
  `G:\stream_record\codex-tmp\.codex-tmp\<任务目录>`；可供用户查看的实验视频、图片、
  报告附件和机器可读结果统一写入
  `G:\stream_record\codex-tmp\.codex-artifacts\<任务目录>`；
- 不得在项目根目录重新创建 `.codex-tmp`、`.codex-artifacts`，也不得借用项目内已有的
  `artifacts`、`target`、`build` 等目录存放 Codex 实验产物；
- 用户明确指定其他测试目录时以用户指定目录为准。清理或迁移时只处理能够确认由
  Codex 生成的文件，不移动、覆盖或删除项目原有产物。
