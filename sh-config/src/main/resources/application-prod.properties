spring.profiles.active=prod
# redis??
spring.redis.host=${spring.redis.host}
spring.redis.port=${spring.redis.port}
spring.redis.password=${spring.redis.password}
spring.redis.database=${spring.redis.database}
# mysql??
spring.datasource.username=${spring.datasource.username}
spring.datasource.password=${spring.datasource.password}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
# ocr????
ocr.server.host=${ocr.server.host}
ocr.server.port=${ocr.server.port}
ocr.server.token=${ocr.server.token}
# ????api
live.api.server.host=${live.api.server.host}
live.api.server.port=${live.api.server.port}
# alist????
alist.server.host=${alist.server.host}
alist.server.username=${alist.server.username}
alist.server.password=${alist.server.password}
alist.server.port=${alist.server.port}
streamerrecord.io.name=${streamerrecord.io.name}
streamerrecord.io.password=${streamerrecord.io.password}
# ???????
wecom.corp.id=${wecom.corp.id}
wecom.agent.id=${wecom.agent.id}
wecom.agent.secret=${wecom.agent.secret}
wecom.event.token=${wecom.event.token}
wecom.event.encoding-aes-key=${wecom.event.encoding-aes-key}
# ???????secret
message.receive.secret=${message.receive.secret}
# ????????
wecom.webhook.secret=${wecom.webhook.secret}
# ??????
system.env.flag=${system.env.flag}
system.storage.mounted=${system.storage.mounted}

# mvc??
server.address=0.0.0.0
server.port=8080
server.servlet.context-path=/api
# ??????
logging.file.path=/home/admin/stream/logs
sh.init-config.path=/home/admin/stream/config/init.json
sh.video-save.path=/home/admin/stream/download
sh.account-save.path=/home/admin/stream/account
# playwright??
playwright.headless=true
# mybatis??
mybatis.mapper-locations=classpath:mapper/*.xml
mybatis.type-aliases-package=com.sh.config.dao

# ASR配置
asr.provider=${asr.provider}

# Aliyun DashScope ASR配置
asr.aliyun.api-key=${asr.aliyun.api-key}
asr.aliyun.model=${asr.aliyun.model}

# OSS配置
oss.provider=${oss.provider}

# Aliyun OSS配置
aliyun.oss.endpoint=${aliyun.oss.endpoint}
aliyun.oss.bucket=${aliyun.oss.bucket}
aliyun.oss.access-key-id=${aliyun.oss.access-key-id}
aliyun.oss.access-key-secret=${aliyun.oss.access-key-secret}

# Highlight Analysis配置
# Provider: none (default), langchain4j (via LangChain4j)
highlight.analysis.provider=${highlight.analysis.provider}

# LangChain4j AI Configuration (OpenAI-compatible API)
# Supports: DeepSeek, OpenAI, and other compatible providers
langchain4j.open-ai.api-key=${langchain4j.open-ai.api-key}
langchain4j.open-ai.base-url=${langchain4j.open-ai.base-url}
langchain4j.open-ai.model-name=${langchain4j.open-ai.model-name}

